@page
@model Sistema_de_Gestion_de_Proyectos_y_Tareas.Pages.Configuracion.CambiarPasswordModel
@{
    ViewData["Title"] = "Cambiar Contraseña";
    var requiereCambio = User.FindFirst("RequiereCambioContraseña")?.Value == "True";
}

<style>
    :root {
        --accent-red: #D32F2F;
        --accent-orange: #FF5722;
        --accent-gold: #FFD700;
        --accent-green: #4CAF50;
        --text-primary: #f5f5f7;
        --bg-dark: #2c2c31;
    }

    .alert-password-required {
        ...
    }

    .requirements-box {
        ...
    }

    .title-volcanic {
        ...
    }

    .form-control {
        ...
    }

        .form-control:focus {
            ...
        }

        .form-control.is-valid {
            ...
        }

        .form-control.is-invalid {
            ...
        }

    .form-label {
        ...
    }

    .text-danger {
        ...
    }

    .form-container {
        ...
    }

    .btn-volcanic-primary {
        ...
    }

        .btn-volcanic-primary:hover {
            ...
        }

    .btn-volcanic-secondary {
        ...
    }

        .btn-volcanic-secondary:hover {
            ...
        }

    .info-box {
        ...
    }

        .info-box strong {
            ...
        }

    .password-requirements {
        ...
    }

        .password-requirements h6 {
            ...
        }

    .requirement-item {
        ...
    }

        .requirement-item.met {
            ...
        }

        .requirement-item.not-met {
            ...
        }

        .requirement-item i {
            ...
        }

        .requirement-item.met i::before {
            ...
        }

        .requirement-item.not-met i::before {
            ...
        }

    .password-strength {
        ...
    }

    .strength-weak {
        ...
    }

    .strength-medium {
        ...
    }

    .strength-strong {
        ...
    }

    .password-toggle {
        ...
    }

        .password-toggle:hover {
            ...
        }

    .input-wrapper {
        ...
    }

    .text-success {
        ...
    }
</style>

@if (!string.IsNullOrEmpty(TempData["MensajeExito"] as string))
{
<div class="alert alert-success alert-dismissible fade show" role="alert">
    <strong>Éxito:</strong> @TempData["MensajeExito"]
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
}

@if (!string.IsNullOrEmpty(TempData["MensajeError"] as string))
{
<div class="alert alert-danger alert-dismissible fade show" role="alert">
    <strong>Error:</strong> @TempData["MensajeError"]
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
}

@if (requiereCambio)
{
<div class="alert-password-required">
    <h3>CAMBIO DE CONTRASEÑA REQUERIDO</h3>
    <p><strong>Por tu seguridad, debes cambiar tu contraseña temporal ahora.</strong></p>
    <p>Estás usando una contraseña temporal generada automáticamente. Por políticas de seguridad de la empresa, debes crear una contraseña nueva y personalizada antes de continuar usando el sistema.</p>
    <div class="requirements-box">
        <strong style="color: #ff5722;">La nueva contraseña debe cumplir:</strong>
        <ul>
            <li>Entre <strong>8 y 15 caracteres</strong></li>
            <li>Al menos una letra <strong>MAYÚSCULA</strong></li>
            <li>Al menos una letra <strong>minúscula</strong></li>
            <li>Al menos un <strong>número</strong></li>
            <li>Al menos un <strong>símbolo especial</strong></li>
        </ul>
    </div>
</div>
}

<h1 class="title-volcanic">@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-8 form-container">
        <div class="info-box">
            <strong>Seguridad de la Contraseña</strong>
            Cambia tu contraseña regularmente para mantener tu cuenta segura.
        </div>

        <div class="password-requirements">
            <h6>Requisitos de la contraseña:</h6>
            <div class="requirement-item" id="req-length"><i></i><span>Entre 8 y 15 caracteres</span></div>
            <div class="requirement-item" id="req-uppercase"><i></i><span>Al menos una letra mayúscula (A-Z)</span></div>
            <div class="requirement-item" id="req-lowercase"><i></i><span>Al menos una letra minúscula (a-z)</span></div>
            <div class="requirement-item" id="req-number"><i></i><span>Al menos un número (0-9)</span></div>
            <div class="requirement-item" id="req-special"><i></i><span>Al menos un carácter especial (@@, #, $, %, etc.)</span></div>
        </div>

        <form method="post" id="cambiarPasswordForm">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="Input.ContraseñaActual" class="form-label"></label>
                <div class="input-wrapper">
                    <input asp-for="Input.ContraseñaActual" class="form-control" type="password" id="contraseñaActualInput" placeholder="Ingresa tu contraseña actual" />
                    <span class="password-toggle" onclick="togglePassword('contraseñaActualInput')">Mostrar</span>
                </div>
                <span asp-validation-for="Input.ContraseñaActual" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Input.NuevaContraseña" class="form-label"></label>
                <div class="input-wrapper">
                    <input asp-for="Input.NuevaContraseña" class="form-control" type="password" id="nuevaContraseñaInput" placeholder="Crea una contraseña segura" oninput="validatePassword()" />
                    <span class="password-toggle" onclick="togglePassword('nuevaContraseñaInput')">Mostrar</span>
                </div>
                <span asp-validation-for="Input.NuevaContraseña" class="text-danger"></span>
                <div id="passwordStrength" class="password-strength" style="display: none;"></div>
            </div>

            <div class="mb-3">
                <label asp-for="Input.ConfirmarContraseña" class="form-label"></label>
                <div class="input-wrapper">
                    <input asp-for="Input.ConfirmarContraseña" class="form-control" type="password" id="confirmarContraseñaInput" placeholder="Confirma tu nueva contraseña" oninput="validateConfirmPassword()" />
                    <span class="password-toggle" onclick="togglePassword('confirmarContraseñaInput')">Mostrar</span>
                </div>
                <span asp-validation-for="Input.ConfirmarContraseña" class="text-danger"></span>
                <span id="matchMessage" style="display: none;"></span>
            </div>

            <div class="mt-4">
                <button type="submit" class="btn btn-volcanic-primary">Cambiar Contraseña</button>
                <a asp-page="/Index" class="btn btn-volcanic-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
    setupFormConfirmation('cambiarPasswordForm', 'confirmEdit');

        function togglePassword(inputId) {
            const input = document.getElementById(inputId);
            input.type = input.type === 'password' ? 'text' : 'password';
        }

        function validatePassword() {
            const password = document.getElementById('nuevaContraseñaInput').value;
            const strengthDiv = document.getElementById('passwordStrength');
            const hasLength = password.length >= 8 && password.length <= 15;
            const hasUppercase = /[A-Z]/.test(password);
            const hasLowercase = /[a-z]/.test(password);
            const hasNumber = /\d/.test(password);
            const hasSpecial = /[\W_]/.test(password);

            updateRequirement('req-length', hasLength);
            updateRequirement('req-uppercase', hasUppercase);
            updateRequirement('req-lowercase', hasLowercase);
            updateRequirement('req-number', hasNumber);
            updateRequirement('req-special', hasSpecial);

            const metRequirements = [hasLength, hasUppercase, hasLowercase, hasNumber, hasSpecial].filter(Boolean).length;

            if (password.length > 0) {
                strengthDiv.style.display = 'block';
                if (metRequirements <= 2) {
                    strengthDiv.className = 'password-strength strength-weak';
                    strengthDiv.textContent = 'Contraseña Débil';
                } else if (metRequirements <= 4) {
                    strengthDiv.className = 'password-strength strength-medium';
                    strengthDiv.textContent = 'Contraseña Media';
                } else {
                    strengthDiv.className = 'password-strength strength-strong';
                    strengthDiv.textContent = 'Contraseña Fuerte';
                }
            } else {
                strengthDiv.style.display = 'none';
            }

            validateConfirmPassword();
        }

        function updateRequirement(id, isMet) {
            const element = document.getElementById(id);
            element.classList.toggle('met', isMet);
            element.classList.toggle('not-met', !isMet);
        }

        function validateConfirmPassword() {
            const password = document.getElementById('nuevaContraseñaInput').value;
            const confirm = document.getElementById('confirmarContraseñaInput').value;
            const matchMessage = document.getElementById('matchMessage');
            const confirmInput = document.getElementById('confirmarContraseñaInput');

            if (confirm.length > 0) {
                matchMessage.style.display = 'block';
                if (password === confirm) {
                    matchMessage.className = 'text-success';
                    matchMessage.innerHTML = 'Las contraseñas coinciden';
                    confirmInput.classList.add('is-valid');
                    confirmInput.classList.remove('is-invalid');
                } else {
                    matchMessage.className = 'text-danger';
                    matchMessage.innerHTML = 'Las contraseñas no coinciden';
                    confirmInput.classList.add('is-invalid');
                    confirmInput.classList.remove('is-valid');
                }
            } else {
                matchMessage.style.display = 'none';
                confirmInput.classList.remove('is-valid', 'is-invalid');
            }
        }

        document.addEventListener('DOMContentLoaded', function () {
            document.getElementById('req-length').classList.add('not-met');
            document.getElementById('req-uppercase').classList.add('not-met');
            document.getElementById('req-lowercase').classList.add('not-met');
            document.getElementById('req-number').classList.add('not-met');
            document.getElementById('req-special').classList.add('not-met');
        });
    </script>
}


